# 🐧 恶俗企鹅/高雅人士 表情包制作工具 (Penguin Meme Maker)

一个基于 Vue 3 + Vite 的在线表情包制作工具，支持拖拽文字、自定义字体颜色、导出图片等功能。

郑重申明,本项目的表情包来源于网络,如有侵权我将立马删除

## 🚀 在线使用

本项目已部署在 [Vercel](https://penguin-meme-maker.vercel.app/ ) 上，可直接访问。
![](https://raw.githubusercontent.com/Moeary/pic_bed/main/img/202511260954885.png)
**访问验证：**
为了防止滥用，进入网站前需要进行简单的填空验证。
- 题目：高雅"__ " / 恶俗"__ "
- 答案：`人士` 或 `企鹅` （即双引号内的内容）

### 使用说明

#### 基础操作
1. **选择表情包**：点击左侧表情包库中的任意图片，或者直接**粘贴 (Ctrl+V)** 外部图片到画布。
2. **添加文字**：点击下方"文字工具"（或按 **T** 键），在画布上点击即可输入文字。
3. **导出/复制**：点击下方按钮导出 WebP 图片，或点击复制按钮（快捷键 **Ctrl+C**）直接复制到剪贴板。
4. **查看帮助**：点击底部工具栏的 **❓** 按钮查看详细说明。

#### 元素操作与快捷键
选中画布上的元素（图片或文字）后，你可以：
- **移动**：按住鼠标左键拖拽。
- **缩放**：拖动元素四周的方形手柄（仅单选时）。
- **旋转**：拖动元素顶部的圆形手柄（仅单选时）。
- **多选**：按住 **Ctrl** 键点击多个元素，或按 **Ctrl+A** 全选。
- **删除**：按 **Delete** 或 **Backspace** 键（支持批量删除）。
- **撤销/重做**：**Ctrl+Z** 撤销，**Ctrl+Shift+Z** 重做。
- **切换模式**：按 **T** 键在"元素操作模式"和"插入文字模式"之间切换。

#### 画布设置
- **尺寸 (Size)**：设置导出图片的实际分辨率（默认 512px）。支持自定义大小，适合制作高清表情包。
- **缩放 (Zoom)**：调整画布在屏幕上的显示比例（20% - 300%）。仅改变显示大小，**不影响**最终导出的图片分辨率。

#### 提示
- 双击文字可重新编辑内容。
- 点击画布空白处可取消当前选中。

## 🛠️ 本地开发与部署

如果你想在本地运行或自行部署，请按照以下步骤操作。

### 环境要求
- **Node.js**: `v22.21.1` (推荐使用此版本以确保兼容性)

### 安装与运行

1. **克隆项目**
   ```bash
   git clone https://github.com/Moeary/Penguin-Meme-Maker.git
   cd Penguin-Meme-Maker
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **启动开发服务器**
   ```bash
   npm run dev
   ```
   访问终端显示的地址（通常是 `http://localhost:5173`）。

4. **构建生产版本**
   ```bash
   npm run build
   ```

## 🎨 自定义表情包库 (Meme Base)

如果你想替换默认的企鹅表情包，制作属于自己的表情包生成器，可以按照以下步骤操作：

### 1. 准备图片
准备好你想要使用的图片素材（支持 `.jpg`, `.png`, `.webp` 等格式）。

### 2. 替换图片文件
将你的图片文件放入 `public/meme-base` 文件夹中。
*注意：请先清空该文件夹中原有的图片。*

### 3. 生成清单文件
运行提供的 Python 脚本来自动生成表情包清单 (`meme-manifest.json`)。

```bash
# 确保你已安装 Python 3
python scripts/generate_manifest.py
```

该脚本会扫描 `public/meme-base` 目录下的所有图片，并在 `public/meme-manifest.json` 中生成索引。前端页面会自动读取这个文件来展示表情包列表。

### 4. (可选) 批量处理脚本
如果你有大量原始图片需要裁剪水印或转换格式，可以使用 `scripts/crop_and_convert.py`。
*需自行修改脚本中的 `source_dir` 和 `output_folder` 路径以适配你的文件夹结构。*

## 🎈TODOLIST

1. 考虑使用CDN存储表情包图片,加速访问
2. 在QQ群里面大肆宣传(逃
3. 加入更多的表情?(考虑加入一下做熊猫头的,实在是太经典了)

## 📄 License

MIT License
